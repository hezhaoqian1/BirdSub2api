# 融合渠道监控

## 使用

管理员进入「渠道管理 → 渠道监控 → 融合监控」。创建规则时绑定真实分组、具体模型和已有 V1 探测配置。探测配置必须使用本站该分组的有效专用 API Key，endpoint 与系统设置的前端地址同源。Key 仅用于监控，其日志从该规则的业务统计中排除。主动请求通过本机网关执行，因此继续经过本站鉴权、路由和故障转移。

融合模式默认关闭。开启后，绑定的 V1 配置由融合任务接管，不再由旧任务独立检测。未绑定的 V1 配置和 V2 分析功能继续遵循原有模式。用户入口 `/monitor` 在融合模式开启后展示 V1 风格卡片；管理员保留探测配置和 V2 分析配置标签。

## 状态规则

| 指标 | 绿 | 黄 | 红 |
|---|---|---|---|
| 分钟告警成功率 | ≥95% | ≥60% 且 <95% | <60% |
| 首 Token | <10 秒 | ≥10 且 <25 秒 | ≥25 秒 |

取满足采样条件的指标中最严重状态。成功率只需一个有效请求；被动 TTFT 使用分钟 P50，默认至少五个样本。主动使用单次流式首有效输出时间，HTTP/SSE 错误、非正常终止和 V1 响应校验失败均为红色。推理内容也属于有效输出，单纯角色、头部和心跳事件不计时。

后台每五秒唤醒，评估留出三十秒写入缓冲的上一完整自然分钟。每个规则每分钟只保存一条最终结果；有有效业务样本使用被动统计。当前分钟到第 45 秒仍没有任何业务活动和结果时主动补位，探测先保存为待确认结果，下一分钟评估时优先采用真实业务数据。若只有排除的错误，分钟最终显示未知；下一分钟没有业务活动时仍可主动探测。有流量但 TTFT 样本不足时只显示数值，不单凭该样本判性能异常。

分钟时间按现有日志 `created_at` 归档，主要代表已完成并落账的请求，不是请求开始时间。尚未完成的长请求不在此窗口内；补位探测保存实际执行时间，不声称探测发生在历史分钟。超过缓冲的迟到日志不追溯改变已发出的通知。纯零费用且无 token 的成功响应在现有日志中仍无法与失败占位完全区分；当前以费用或任一 token 计数为成功证据。上线前应核实特殊免费/无用量接口是否属于监控范围。

成功与错误按窗口内 request_id 和 API Key 关联去重；同一请求最终记录了错误时以错误为准，避免出现时间轴红色但成功率仍为 100% 的矛盾。复用 V2 错误分类；非上游归属的认证、非法请求、上下文限制、内容策略、用户取消和分组权限错误从告警分母中排除。用户实际成功率仍保留这些失败，不用 `1 - 过滤错误率` 代替。上游认证/容量/额度问题计入异常。

## 通知与权限

连续三个红色分钟产生故障事件；连续两个绿色分钟产生恢复事件。黄/灰打断红色计数但不宣称恢复；分钟缺口打断连续性。同一故障只创建一次通知，发送失败指数退避重试，最多八次。状态和通知队列均持久化，使用数据库领导锁和唯一键避免多实例重复处理。钉钉不提供幂等消息接口，发送成功但响应丢失或进程恰好退出时仍可能重复投递。

凭证复用 AES-GCM 加密器，不返回明文；Webhook 仅允许官方 HTTPS 机器人路径。通知静默继续计算状态；暂停规则显示维护状态。修改阈值/对象会重置该规则计数，改名称或保存钉钉凭证不重置其他故障。删除、关闭或静默的规则清理未发送通知。

用户接口根据服务端可用分组授权，只返回公开规则；内部 ID、请求量、错误详情、告警状态和通知凭证均被剥离。管理员可查看完整分钟证据与发送记录。

## 性能边界

业务请求不等待分钟统计或钉钉；请求路径仅在尚未记录首 Token 时判断流式事件是否含有效输出。监控仍与业务共享数据库、网关和上游容量，不能承诺零性能影响。分钟日志统计每条规则限制三秒，结果页查询整体限制三秒；超时展示未知，不因统计超时判上游失败。已完成的同一分钟在同一进程内不重复检查全部规则，持久化失败保留重试。历史清理每小时最多删除一万行，并限制两秒。

用户结果页一次批量查询所有已授权规则的分钟结果，不读取原始业务日志；无跨用户响应缓存。页面隐藏时暂停轮询和时间轴刷新，可见页面每三十至三十五秒错峰刷新，时间轴按分钟重算。主动探测最多六个并发、单次四十五秒，仍消耗本站限流额度、上游并发及 token。N 个持续空闲的监控对象理论上每天增加 1440×N 次探测（受调度预算限制）。规则总量最多一百条。高流量生产环境仍需按实际日志量检查查询执行计划，并比较开启前后的网关 P95/P99、数据库 CPU/IO 和连接池等待；当前未做生产规模压测。

## 本地演示

演示服务与正式 Go 后端独立，不访问真实数据库和钉钉，仅绑定 `127.0.0.1`。

```powershell
node tools/hybrid-monitor-demo.mjs
```

另一个终端：

```powershell
cd frontend
$env:VITE_DEV_PROXY_TARGET='http://127.0.0.1:8088'
$env:VITE_HYBRID_DEMO='1'
pnpm dev --host 127.0.0.1 --port 3000
```

- 管理员：`admin@demo.local`
- 普通用户：`user@demo.local`
- 两者密码：`DemoMonitor2026!`
- 管理员路径：`/admin/channels/monitor`
- 用户路径：`/monitor`

模拟数据只保存在进程内，重启恢复。用于 UI 登录、编辑及展示测试，不代表真实上游和钉钉端到端联调。

## 测试

```powershell
cd backend
go test -tags=unit ./internal/service ./internal/handler ./internal/server/routes ./cmd/server -run 'TestHybrid|TestChannelMonitor' -count=1
```

真实 PostgreSQL 测试设置 `HYBRID_TEST_DATABASE_URL`，指向本地隔离测试库。测试创建随机 schema，执行迁移两次、分钟查询、通知去重、重启恢复和用户权限测试，最后仅删除该测试 schema。未设置时跳过该项。

本次本地验证：前端 7 个文件共 35 项测试、类型检查、针对修改文件的 ESLint 和生产构建通过；后端渠道监控、融合监控及相关流式响应回归通过。性能优化后重新运行融合监控测试全部通过，包括隔离 PostgreSQL 的迁移、聚合、批量结果查询、通知去重、重启、权限、配置关闭、版本冲突与状态清理，以及数据库读取失败重试测试；此前 Windows 拒绝执行导致未完成的配置保存验证已补跑通过。前端本轮针对性 7 项测试、类型检查和 ESLint 通过。真实上游请求和钉钉投递尚未端到端验证，未进行生产规模压测。

```powershell
cd frontend
pnpm typecheck
pnpm exec vitest run src/features/channel-monitor-hybrid src/views/user/__tests__/ChannelStatusView.mode.spec.ts
pnpm build
```

当前实现从分钟原始日志查询，避免依赖 V2 五分钟刷新及其历史回填进度。首次启用建议关注实际日志规模下的数据库负载。后台并发探测上限六条，每轮五十五秒预算，拥塞时保留数据缺口，不把监控资源不足伪报成上游故障。
